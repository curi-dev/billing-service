import styled from 'styled-components';

import Box from '@material-ui/core/Box';

export const InfoContainer = styled(Box)`
    border: none;

    max-width: 92%;
    height: 450px;
    
    margin: 0 auto;

    background: #FFF;

    box-shadow: 0 0 .5rem #34353a;
`;

export const ItemContainer = styled.tr`
    background: #c0c0c0;

    height: 50px;
    border: none;
    
`;